{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && { [ \"$VERCEL_ENV\" = production ] && { export DIRECT_URL=\"${DIRECT_URL:-$DATABASE_URL}\"; prisma migrate deploy || prisma db push --skip-generate || true; } || true; } && next build"
}
